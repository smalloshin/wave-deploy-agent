# Decisions Index — wave-deploy-agent

> 所有架構／產品決策的目錄。每個決策獨立一個檔案，放在本目錄。

## 使用規則

1. **每次做出架構或產品決策時**，建立新檔 `YYYY-MM-DD-<slug>.md`
2. **檔名 slug 用 kebab-case**，簡短描述（例：`2026-04-05-stop-via-delete-service.md`）
3. **決策檔內容結構**（參考 ADR 格式）：
   - **Context**：為什麼需要做這個決定
   - **Decision**：決定是什麼
   - **Consequences**：帶來的好處與代價
   - **Status**：Active / Superseded by `<新決策檔>`
4. **被取代的決策不要刪**，把 Status 改成 Superseded 並連到新決策
5. **在本 index.md 登記**：新增一列到下方表格

## 決策狀態說明

| 狀態 | 意義 |
|------|------|
| **Active** | 目前生效中 |
| **Superseded** | 已被其他決策取代，保留作歷史紀錄 |
| **Deprecated** | 已棄用但尚未有取代方案 |
| **Proposed** | 提案中，尚未實作 |

## 決策清單

| 日期 | 決策 | 狀態 | 說明 |
|------|------|------|------|
| 2026-04-05 | [gcs-sources-lifecycle-30d](./2026-04-05-gcs-sources-lifecycle-30d.md) | Active | GCS source tarball 30 天自動刪除，防止無限成長 |
| 2026-04-05 | [artifact-registry-cleanup](./2026-04-05-artifact-registry-cleanup.md) | Active | AR repo keep last 5 tagged + 清 7d untagged / 30d tagged |
| 2026-04-05 | [terraform-disaster-recovery](./2026-04-05-terraform-disaster-recovery.md) | Active | 用 Terraform + bootstrap.sh 實現 agent 一指令重建；secrets 搬到 Secret Manager |
| 2026-04-13 | [netlify-like-versioning](./2026-04-13-netlify-like-versioning.md) | Active | Netlify-like 版本管理：immutable deploy、版本歷史、一鍵 publish/rollback、deploy lock |
| 2026-04-18 | [deployed-source-capture](./2026-04-18-deployed-source-capture.md) | Active | 每次 deploy 成功後把 post-fix code + 自動生成 Dockerfile 存到 GCS (365d lifecycle)，dashboard 一鍵下載，讓使用者從安全基準繼續開發 |
| 2026-04-25 | [deployment-observability](./2026-04-25-deployment-observability.md) | Active | 部署觀測 3 層架構：Tier 1 timeline（7-stage stepper）+ Tier 2 SSE log stream + Tier 3 LLM 診斷（cached by build_id），Tier 4 cost/latency 圖表已 kill |
| 2026-04-25 | [rbac-system-permissive-then-enforced](./2026-04-25-rbac-system-permissive-then-enforced.md) | Active | 全站 RBAC：3 角色（admin/reviewer/viewer）+ 16 權限 + bcrypt password + SHA-256 session + API key + audit log；零停機 permissive→enforced 兩段切換 |
| 2026-04-26 | [live-build-log-streaming](./2026-04-26-live-build-log-streaming.md) | Active | Build log 從 post-mortem 升級成 live tail：`onBuildStarted` callback 早 leak buildId，背景 task 把 `pollBuildLog` chunks 透過 SSE 廣播到既有 deployment-event-stream |
| 2026-04-27 | [chunked-upload-defaults](./2026-04-27-chunked-upload-defaults.md) | Active | Round 30 緊急 fix：chunked GCS resumable upload 預設值收緊（1 MiB chunks / 15 retries / 60s backoff cap / 120s XHR timeout）解 vibe-coded user 慢連線 426 MB 檔案上傳卡 33% 問題 |
| 2026-04-27 | [rbac-scope-list-pattern](./2026-04-27-rbac-scope-list-pattern.md) | Active | Round 31 RBAC LIST IDOR 修法 pattern：`scopeForRequest(auth,mode) → ListXxxScope` + `buildListXxxSql(scope)` 三態 pure helpers，filter 在 SQL 不在 app code，避免 timing channel；R32–R36 follow-through 收完所有 IDOR（4 P0 list + 6 P1 single + 1 bulk mutating） |
| 2026-04-27 | [bot-api-key-bootstrap](./2026-04-27-bot-api-key-bootstrap.md) | Active | Round 37 Bot RBAC consumer wiring：抽 `auth-headers.ts` 純 helper（22 LOC）+ 18 個 zero-dep 測試鎖 `Bearer <key>` wire shape；boss-gated 下一步是決定 bot user identity（reviewer SA / 共用 admin）+ 發 api_keys row |
| 2026-04-27 | [permission-check-shared](./2026-04-27-permission-check-shared.md) | Active | Round 38 RBAC predicate 移到 `@deploy-agent/shared`：`hasPermission` / `effectivePermissions` / `checkUserPermission` 三個純函式 + 38 zero-dep 測試（含 7 條 server/client parity contract + 3 條 escalation regression）；sweep 腳本擴充覆蓋 `packages/*/src/` |
| 2026-04-27 | [upload-error-mapper-test-lock](./2026-04-27-upload-error-mapper-test-lock.md) | Active | Round 39 web 上傳錯誤翻譯層 wire-contract lock：`upload-error-mapper.ts`（242 LOC，4 個 exported pure helpers）+ 172 zero-dep 測試（鎖每個 `UploadFailureCode` 對 i18n key、`mapClientError` 啟發式順序、`fetchDiagnostic` 永不 throw 契約、`buildErrorReport` 條件區塊、426 MB 檔案的 "426.5 MB" 顯示字串）；sweep 腳本擴充覆蓋 `apps/web/lib/`，順帶把 R27/R30 孤兒 `test-resumable-upload.ts` (91) 拉進 gate |
| 2026-04-27 | [upload-draft-storage-test-lock](./2026-04-27-upload-draft-storage-test-lock.md) | Active | Round 40 web localStorage 草稿層 wire-contract lock：`upload-draft-storage.ts`（127 LOC，5 個 exported helpers）+ 55 zero-dep 測試（in-memory localStorage shim；鎖 7-day expiry 數字 `604800000ms`、schema v:1 migration 安全網、50 KB cap 降級保留 formData 丟 fileMeta、`gcExpiredDrafts` 不碰 non-prefix key、debounce「last value wins + 只 fire 一次」契約、SSR isBrowser no-op 護欄）|
| 2026-04-27 | [cost-estimator-test-lock](./2026-04-27-cost-estimator-test-lock.md) | Active | Round 41 GCP cost estimator wire-contract lock：`cost-estimator.ts`（108 LOC，2 個 exported pure helpers）+ 60 zero-dep 測試（pricing 常數釘死在測試與 source 兩處強制 review；free-tier `Math.max(0,...)` clamps 每個獨立驗證；2-decimal rounding；`currency: 'USD' as const` literal contract；minInstances always-on math（30×24×3600 sec/month）；formatCostEstimate 8-line 輸出格式 + integer 不出 `.00`）|
| 2026-04-27 | [state-machine-test-lock](./2026-04-27-state-machine-test-lock.md) | Active | Round 42 shared state-machine 規則 + 4 個 state-classification predicates wire-contract lock：`state-machine.ts`（129 LOC，6 個 exported helpers + 2 個 error class）+ 100 zero-dep 測試（PINNED_TRANSITIONS 15×15 = 225-cell adjacency matrix 釘死在測試與 source 兩處；`getValidTransitions` 每個 state 顯式 + reference identity；`isTerminalState`/`isActionableState`/`requiresHumanAction` 每個 state 顯式 + 集合 cardinality + `requiresHumanAction ⊆ isActionableState` / `terminal ∩ actionable = ∅` invariants；R12 `Invalid state transition:` string-match 契約；R12 `ConcurrentTransitionError` field 保留；planner 全 sweep 不能 diverge from rules）|
| 2026-04-27 | [discord-audit-writer-test-lock](./2026-04-27-discord-audit-writer-test-lock.md) | Active | Round 43 bot Discord NL audit-trail writer wire-contract lock：`discord-audit-writer.ts`（101 LOC，2 個 async fetch wrappers）+ 53 zero-dep 測試（globalThis.fetch mock + env-var priming + dynamic import；POST `/api/discord-audit` URL/method/Content-Type/body 每欄位釘死 + `status: 'pending'` literal；PATCH `/api/discord-audit/:id` URL formatting + 4 個 AuditStatus 全測；`id === null` short-circuit 鎖死；`typeof json.id === 'number'` typecheck 鎖死含 id=0/-1 邊界；silent-failure 三條全部 fetch throw / non-200 / json throw 不 propagate；nested object body 證明 JSON.stringify wire shape）|
| 2026-04-28 | [upload-trans-pacific-rescue](./2026-04-28-upload-trans-pacific-rescue.md) | Active | Round 44 跨太平洋上傳救援：(1) `xhr.onerror` 從 `Error('network')` 升級成 `Error & XhrErrorDiagnostic`（攜帶 `xhrStatus`/`xhrStatusText`/`xhrResponseURL`/`xhrResponseText`），bail-out `lastError` 序列化成 `network status=N statusText="..." url=... body="..."` 給 log/LLM diagnostic 用；(2) 新 `POST /api/upload/verify` 直接查 GCS object metadata（size + md5），權限 `projects:write` 帶 bucket prefix guard；(3) `uploadResumable` 加 optional `verifyComplete` callback，retry exhaust 前問 GCS 物件存不存在，size match 就 short-circuit `ok: true`，rescue trans-Pacific 「bytes 已到、200 response 在跨太平洋回程被切」case；27 新 zero-dep 測試（123/0 total）；ADR 已標記 R45 跟進（bucket 搬 asia-east1）|
| 2026-04-30 | [llm-primary-gpt55](./2026-04-30-llm-primary-gpt55.md) | Active | LLM provider 翻轉：GPT-5.5 為主、Claude 為備（3 個 callsite — llm-analyzer / resource-analyzer / nl-handler — 翻轉嘗試順序；`OPENAI_MODEL` 預設 `gpt-5.4` → `gpt-5.5`；nl-handler fallback trigger 加 `quota`；2 份 .env.example 同步） |
| 2026-04-30 | [r44f-pipeline-worker-normalize](./2026-04-30-r44f-pipeline-worker-normalize.md) | Active | R44f：pipeline-worker GCS 重抓路徑加 `normalizeExtractedPaths` + `descendIntoWrapperDir`；AI fix step `fix.filePath` 用新 `sanitizeRelativePath`（反斜線→正斜線 / 剝 drive letter / 剝 wrapper prefix / 拒 `..`）；archive-normalizer 從 1 export 擴成 3 export；測試 50 → 96 zero-dep 全綠 |
| 2026-04-30 | [r44g-prisma-auto-fix](./2026-04-30-r44g-prisma-auto-fix.md) | Active | R44g：偵測 Prisma 專案（`@prisma/client` deps / `schema.prisma` / `prisma.config.*` 任一）→ pipeline-worker Step 2 對使用者 Dockerfile 注入 `RUN DATABASE_URL="file:/tmp/..." npx prisma generate`（builder stage 在 build 前），auto-gen Dockerfile 也加；純函式 + idempotent + 拒 newline/quote 注入；56 個 zero-dep 測試 + 6 個 dockerfile-gen 新測 |
| 2026-04-30 | [r44h-strictness-flip-guard](./2026-04-30-r44h-strictness-flip-guard.md) | Active | R44h：兩道防線解 R44g 後 legal-flow 仍掛的 type-check 死 — (1) Step 5 加 `isStrictnessFlip` 擋住 LLM auto-fix 把 `ignoreBuildErrors`/`ignoreDuringBuilds` 從 `true→false` 翻轉（LLM 沒看完整 type-check output，翻了會把使用者藏起來的錯誤一次曝光弄死 build）；(2) Step 2 加 `detectNextMajorVersion` + `stripEslintFromNextConfig`，偵測 Next major ≥ 16 自動砍 `next.config.{ts,js,mjs}` 的 deprecated `eslint:{}` block（Next 16 把該 key 從 NextConfig type 移除，殘留就 `'eslint' does not exist in type 'NextConfig'`）；balanced-brace walker 是 string/comment-aware；63 個 zero-dep 測試（14 flip + 13 version + 19 strip + 9 brace + 8 output shape）|
| 2026-04-30 | [wave-deploy-skill-marker-routing](./2026-04-30-wave-deploy-skill-marker-routing.md) | Active | `/wave-deploy` Claude Code skill v0.1：marker file (`.deploy-agent.json`) 為核心的智慧路由 — `bin/wave-deploy-detect` bash + jq script 輸出四種 match level（high / stale / medium / none）；後續部署 1-click 確認；不主動 approve 安全審查；jq `select(. != "")` 陷阱用 `def nz: if . == "" then null else . end;` helper 解決；marker URL 用 deployment.customDomain（要自己加 https://）優先於 cloudRunUrl |
| 2026-05-01 | [review-gate-toggle](./2026-05-01-review-gate-toggle.md) | Active | R45：settings 頁面加 `requireReview` 開關 — ON（預設）走原審查邏輯，OFF 時 scan + LLM 照樣跑、產生 review record，但 pipeline 自動 approve（reviewer=`system`）並直接 dispatch deploy；新 `settings-service.ts` parser 純函式 + 10 個 zero-dep 測試鎖 edge case；`routes/settings.ts` GET/PUT merge loop 改 type-aware（`if (value &&)` 會吞 false）；UI 用 `<button role="switch">` toggle；State machine 不變（`review_pending → approved` 本來就合法），audit trail 完整 |
| 2026-05-02 | [cloud-run-port-auto-fix](./2026-05-02-cloud-run-port-auto-fix.md) | Active | R46：Cloud Run PORT 兩種 failure mode 自動修 — (1) user Dockerfile hardcode `--port 8080` 但 Cloud Run 給 `PORT=8000` → health check timeout（luca-optimizer-kb canonical）；(2) exec form `CMD [..., "--port", "${PORT:-8000}"]` 不會展開（dockerfile-gen.ts 自己也中招）。新 `dockerfile-port-fixer.ts` deterministic Step 2 fixer + 31 zero-dep 測試（5 種 port flag 形式、idempotent、preserve whitespace、defensive 全綠）；同步修 `dockerfile-gen.ts` Python startCmd 走 `sh -c` 包裝（`python main.py` 例外，無變數可展）；架構同 R44g prisma-fixer / R44h next-config-fixer pattern，不走 LLM（延遲 + 不確定性 + R44h 才剛擋 LLM 翻 strictness flag） |
| 2026-05-03 | [reconciler-cloudrun-url-recovery](./2026-05-03-reconciler-cloudrun-url-recovery.md) | Active | R47：reconciler false positive 修法 — wavenet-ai-gateway-frontend 被誤標 failed（Cloud Run 其實活著），root cause 是 `deploy-worker.ts:893` 單次 DB write 偶發掉造成 `cloudRunUrl` 沒寫進去。Two-part fix：(1) 該行包 3-attempt retry（100/300/900 ms）防止 root cause 再發生；(2) 新 `reconciler-recovery.ts` pure decider（8 條分支 verdict）+ `getCloudRunServiceTruth` helper，reconciler 看到 `cloudRunUrl` 空時先去 Cloud Run 校對：service ready + condition succeeded + revision 一致就 back-fill cloudRunUrl 而不是 mark failed。Race window 6 min 防止跟 deploy-worker 搶寫；zombie protection 要求 `liveRevision === deployment.revisionName`；架構純函式 + 22 zero-dep 測試把 8 條分支全鎖死 |
| 2026-05-03 | [lockfile-arbiter](./2026-05-03-lockfile-arbiter.md) | Active | R48：luca-web 那類「pnpm 開發 / 卻 commit 了 stale package-lock.json」自動辨識 — 新 `lockfile-arbiter.ts` pure decider，按 corepack 宣告（`package.json#packageManager`）→ workspace 訊號（`pnpm-workspace.yaml`）→ 多 lockfile mtime tie-break → 單 lockfile → 預設 npm low-confidence 順序判斷 PM；輸出 `{packageManager, confidence, reason, warnings}`；`project-detector.ts` 加兩個 additive 欄位 `packageManagerConfidence`/`Warnings`；`dockerfile-gen.ts` npm low-confidence 用 `npm install --no-audit --no-fund` 取代會炸的 `npm ci`，pnpm 分支多 COPY `pnpm-workspace.yaml*` 讓 workspace 專案 build；33 個 zero-dep 測試鎖死 5 條 priority 路徑 + edge cases；不加 Docker-side install retry（會掩蓋真 bug） |
| 2026-05-03 | [required-env-gate](./2026-05-03-required-env-gate.md) | Active | R49：Python FastAPI 那類「container 啟動時 KeyError 在 missing env var」自動處理（luca-optimizer-kb / wavenet-ai-gateway-backend canonical）— 新 `python-env-extractor.ts` pure 掃描器辨識 `os.environ["X"]`(required)、`os.getenv("X")`(optional, 但 if-not-raise 會 upgrade)、Pydantic `BaseSettings` 必填欄位、Secret Manager `access_secret_version`，含 string masking 防 docstring/字串內容假陽性；新 `required-env-gate.ts` pure decider，**窄白名單**（`*JWT*SECRET*` / `*SESSION_SECRET*` / `NEXTAUTH_SECRET` / `*SIGNING_KEY*` / `*ENCRYPTION_KEY*` / `*CSRF*SECRET*`）走 auto-gen，**deny list**（`*PASSWORD*` / `*_API_KEY` / `STRIPE_*` / `OPENAI_*` / `DATABASE_URL` 等 paired credentials）強制 block；`settings-service.ts` 加 `autoGenerateSecrets` toggle 預設 OFF（operator opt-in）；兩個 enforcement 點：pipeline-worker Step 2.7（早期擋住省 3-5 分鐘）+ deploy-worker pre-build（defense in depth 防 LLM 過度樂觀）；68 個 zero-dep 測試 |
| 2026-05-03 | [reconciler-race-window-extension](./2026-05-03-reconciler-race-window-extension.md) | Active | R51：reconciler race window 6→15 min — 實測 `wavenet-ai-gateway-frontend` Next.js + npm install 整條 deploy 跑 ~9 分鐘，R47 的 6 分鐘 race window 在第 7 分鐘提早判「卡死」mark failed（其實 deploy-worker 還在 Cloud Build 等 image build 完）。15 分鐘涵蓋大型 Next.js / Python build。真卡死的 deploy 多等 9 分鐘介入 OK（沒 Cloud Run service 啟動 = 沒 user impact），用 false-positive=0 換 9 分鐘等待時間 ROI 巨大 |
| 2026-05-03 | secret-manager-detection (inline R52) | Active | R52（improvement to R49）：(a) python-env-extractor regex `secrets/([A-Z_][A-Z0-9_]*)/versions` → `secrets/([a-zA-Z][a-zA-Z0-9_-]*)/versions`，支援 wavenet-ai-gateway-backend 的 `erp-jwt-secret`（小寫+連字號，常見 GCP 命名）。(b) required-env-gate 加 `source === 'secret-manager'` 的 early-block check，**永遠 block** 不論 name 命中哪個白/黑名單 — 理由：app 從 Google Secret Manager API 讀，不從 env vars，auto-gen 寫進 envVars 對這條 code path 完全沒效。Block 的 hint 提供 actionable `gcloud secrets create X --data-file=- <<<...` + IAM binding 指令。+7 zero-dep 測試 |
| 2026-05-04 | [cloud-run-container-logs-in-llm-diagnosis](./2026-05-04-cloud-run-container-logs-in-llm-diagnosis.md) | Active | R53：dashboard LLM diagnosis product gap 修復 — wavenet-ai-gateway-backend 部署失敗 dashboard 顯示「可能是 FastAPI 啟動失敗，請本地 debug」（generic）但實際 root cause 是 Python `RuntimeError: erp-jwt-secret is required but not available from Secret Manager`。原因：deploy-worker 失敗時只把 deploy-worker 自己的 error message 餵 LLM，沒撈 Cloud Run container 的 stderr/stdout（traceback 在那）。新 `cloud-run-logs-fetcher.ts` 純函式 `extractCloudRunMetaFromError`（從 error 提取 service+revision，URL-encoded + quoted form 兩種都認）+ async `fetchContainerLogs`（Cloud Logging API entries:list，10min lookback，severity≥DEFAULT 涵蓋 stdout+stderr）+ `formatLogEntries`（反序、`[ts severity] text` 格式、30KB cap 從頭截保留尾巴）。outer catch 在 `currentStep includes 'Cloud Run'` 時 prepend 30KB container logs 到 attachedLog。Best-effort never-throws。+19 zero-dep 測試 |
| 2026-05-04 | reanalyze-failure-also-uses-container-logs (inline R54) | Active | R54（R53 follow-up）：`/api/projects/:id/reanalyze-failure` endpoint 也走同款 cloud-run-logs-fetcher。原本 reanalyze 只 refetch Cloud Build log（only Step 3 失敗有 Cloud Build ID）；R54 補上 — 當 `failedStep includes 'Cloud Run'` 時呼叫 R53 的 fetcher 拉 container logs prepend 給 LLM。讓 user 在 dashboard 點「重新診斷」對任何**現有**失敗的 Cloud Run deploy 拿到 R53-quality 的精確診斷，不用 redeploy |
| 2026-05-04 | [env-file-scanner](./2026-05-04-env-file-scanner.md) | Active | R55：R49 false-positive 修法 — `luca-v2-20260504-luca-optimizer-kb` source 裡有 `.env` 包含 `GEMINI_API_KEY=AIzaSy...` 但 R49 只看 `project.config.envVars`（dashboard 設的）沒讀 `.env`，誤判 missing 在 1.3 秒就 block 了。新 `env-file-scanner.ts` pure function 讀 `.env`/`.env.local`/`.env.production`/`.env.staging`/`.env.development`（**排除** `.env.example`/`.env.sample`/`.env.template` 等模板），parse `KEY=VALUE`（容忍 comments / quoted / `export` prefix / inline `#` / CRLF）merge 進 R49 的 `userProvidedKeys`。**Bonus security**：偵測真實 secrets commit（OpenAI sk- / Google AIza / Meta EAA / GOCSPX- / GitHub ghp_ / long hex），warn operator 該 rotate + .gitignore（不 block，user 已 commit 阻止太晚）。Pattern 順序敏感（specific 在前避免 generic 搶匹配）。+43 zero-dep 測試 |
| 2026-05-05 | [pre-deploy-migration](./2026-05-05-pre-deploy-migration.md) | Active | R57：deploy-worker Step 3.5 在 Cloud Build 之後、Cloud Run revision swap 之前跑 DB migration（Prisma + Alembic v1）。新 `migration-detector.ts` pure 偵測 + `gcp-poll.ts` DRY helper（R47 也可重用）+ `cloudrun-jobs-migration-runner.ts` 用 Cloud Run Jobs orchestration。Concurrency = row-based primitive in `wave_deploy_migrations` table（reviewer 5.1：advisory lock 沒 TTL 連線斷會釋放 race window 真實，row + unique partial index + expires_at TTL 取代）。Prisma CLI 保留在 prod stage 透過 R44g `ensurePrismaCliInProd()` extend。Toggle `settings.runMigrations` 預設 false（reviewer 2.4：default-on 跟 v1 no rollback 不一致 operator 必須 opt-in）。Stage_events 加 `'migrate'` 插在 push/deploy 之間。Zero-downtime：失敗時舊 revision 保留 100% 流量。+44 zero-dep 測試 |
| 2026-05-07 | [honest-monorepo-descent](./2026-05-07-honest-monorepo-descent.md) | Active | R60：Pipeline 的 monorepo descent 太 eager — root 沒 Dockerfile + 一個 subdir 有 Dockerfile 時自動 descend 進去，砍掉 sibling（`wavenetdeveloper-rfp-agent` canonical：`frontend/index.html` 53 KB 被丟掉，FastAPI `app.mount("/", StaticFiles("../frontend"))` 默默 fail）。新 `services/monorepo-strategy.ts` 5-branch pure decider 取代既有 `routes/projects.ts:786` 的天真 descent — `honest-monorepo` strategy 不 descend，build context = root，記下 `dockerfilePath = '<sub>/Dockerfile'` 給 Cloud Build `-f` flag。Plumbing：`ProjectConfig.dockerfilePath?` 新 field、`deploy-engine.ts` `-f` 條件 emit、`pipeline-worker.ts` Step 2 fixers (R44g/R46) 用 `dockerfileAbsPath`、`routes/versioning.ts` new-version 路徑 re-evaluate strategy。Backward compat 0 影響（多數 project `dockerfilePath` undefined 走原路）。+17 zero-dep 測試（5 branches + edge cases + reference identity + forward-slash 路徑）|
| 2026-05-11 | [user-facing-secret-detection](./2026-05-11-user-facing-secret-detection.md) | Active | R64：legal-flow-20260505 canonical — user 寫 `SYSTEM_PASSWORD="admin123"` 在 `.env`，pipeline silent auto-gen 強隨機字串，user 打 admin123 → 401 完全不知道為什麼。R64 拆 third classification path：既有 strict `isUserCredentialVar` keep-verbatim path 不動；新 broader `isUserFacingCredential` （含 SYSTEM/LOGIN/APP/MASTER/ROOT/SHARED/GUEST/DEMO/TEST/USER/SUPER/ACCOUNT 共 12 個 prefix + PIN/PASSPHRASE suffix）weak 時走 **auto-gen + P0 警告 + reveal endpoint** 三步。新 `EnvWarning.type='user_facing_weak_replaced'` + `severity='p0_user_action_needed'`。新 `GET /api/projects/:id/env-vars/reveal`（`requireOwnerOrAdmin` + 既有 `auth_audit_log` table 記每次 reveal + 可選 `?keys=A,B,C` 窄化）讓 deployer 看 auto-gen 值或 PATCH 覆寫。Cloud Run live env 優先、DB fallback、source field 表示來源。+39 zero-dep tests（strict back-compat 5 + R64 expanded 12 + PIN/PASSPHRASE 5 + case-insensitive 2 + machine-secret negative 12 + 防 `PASSWORDLESS_TOKEN` word-boundary 誤命中等 defensive 3） |
| 2026-05-08 | [project-group-attach-endpoint](./2026-05-08-project-group-attach-endpoint.md) | Active | R63：新 `PATCH /api/projects/:id/group` endpoint 讓 owner/admin 把 single-service project 連到既有 project group。Pre-R63 group 只能透過 submit-gcs monorepo split 建立（≥2 subdirs 都有 Dockerfile），分次 submit 的 sibling 各自 singleton group。canonical：rfp-agent (API) + rfp-agent-frontend (UI) 分兩次 submit-gcs，pre-R63 沒辦法 attach。Body `{ projectGroup: string≤100, groupName?: string≤200 }`，純 config jsonb merge，不動 Cloud Run/schema/deploy state。`requireOwnerOrAdmin` auth gate。不寫 route test（codebase 慣例：純 helpers 才單測，薄 validation route 走 e2e） |
| 2026-05-07 | [archive-extractor-shared](./2026-05-07-archive-extractor-shared.md) | Active | R62：抽 shared `services/archive-extractor.ts` helper（pure `detectArchiveFormat` + async `extractArchive` discriminated result，never throws），同步 submit-gcs 跟 new-version 兩個 routes 對 archive format 支援不一致的問題（new-version pre-R62 只認 `.tar.gz`，user 上傳 `.zip` 撞 `gzip: stdin has more than one entry`）。兩個 routes 改成呼叫 helper，archive format 增加只改一處。+14 zero-dep tests（detectArchiveFormat 純函式 5 case + extractArchive round-trip 含 zip/tar.gz/tgz/tar + unsupported_format + corrupt archive + missing file 不 throw） |
| 2026-05-07 | [r57-followups](./2026-05-07-r57-followups.md) | Active | R57.1+R57.2+R57.3 一次解：(1) reconciler race window 從 R51 的 15 min 升到 25 min 應對 R57 加 migration step 後 deploy 漲到 12-15 min；(2) 新 `services/migration-cleanup.ts` pure 函式 + 11 zero-dep tests 處理 `wave_deploy_migrations` table TTL sweep（`status='running'` AND `expires_at < NOW()` → 'expired'，解 worker crash 卡 unique partial index）+ retention delete（30 天前 row）；reconciler 每 30 tick（每小時）跑一次 `runMigrationCleanup`；(3) detector 從 4 verdict 擴到 9：加 Django (`manage.py` + Django dep) → `python manage.py migrate --noinput`、Flask-Migrate (`Flask-Migrate` dep) → `flask db upgrade`、Drizzle (`drizzle.config.*` + `drizzle-kit` dep) → `npx drizzle-kit migrate`、Knex (`knexfile.*` + `knex` dep) → `npx knex migrate:latest`、TypeORM (typeorm dep + `data-source.*`) → manual skip + warning。新 helpers `hasPythonDep` (regex match requirements.txt + pyproject.toml，加 `(?![A-Za-z0-9_-])` lookahead 防 `django-rest-framework` 誤命中) + `hasNodeDep` (package.json deps/devDeps exact match)。Cloud Run Job runner early-skip 改成 `verdict.command === null` 涵蓋 none/db_push_only/typeorm_manual。+25 zero-dep 測試 |
| 2026-05-07 | [dockerignore-copy-conflict](./2026-05-07-dockerignore-copy-conflict.md) | Active | R61：pre-build detector 抓 Dockerfile `COPY <src>` 引用 `.dockerignore` 排除的檔（`legal-flow-20260505` canonical：`.dockerignore` 第 6 行 `.env*.local` AND Dockerfile 第 32 行 `COPY .env.local .env.local` → Cloud Build 跑 4 分鐘到 Step 22/27 才 fail）。新 `services/dockerignore-conflict-detector.ts` pure 函式 + glob matcher（`*`/`**`/`?`/`!` negation/trailing-slash dir/不跨 `/`）+ Dockerfile parser（剝 `--chown=`、跳過 `--from=` multi-stage、跳過 `ADD https://`、處理 quoted 跟多 src）。`pipeline-worker.ts` Step 2 後跑 detector，**warning 不 block**（R44h 精神），結果寫進 `project.config.dockerignoreConflicts` JSONB 給 dashboard 用。+21 zero-dep 測試（canonical case + glob edges + negation order + COPY 解析變體 + edge cases） |
| 2026-05-07 | [vite-static-detection](./2026-05-07-vite-static-detection.md) | Active | R59：`dockerfile-gen.ts` 對 Vite 沒偵測，`bid-ops-frontend` canonical：generic Node SSR template 寫 `CMD ["node", "dist/index.js"]` 但 Vite 只 build 出靜態檔，container 啟動 4 分鐘 Cloud Run timeout。`project-detector.ts` 加 Vite 偵測（`deps['vite']` AND `vite.config.{ts,js,mjs}` 存在）→ `framework: 'vite-static'`。新 `generateViteStaticDockerfile` 多階段 build：Node 跑 `npm run build`、nginx:alpine serve 靜態檔、sed 替換 `listen 80;` 為 `listen ${PORT};` 響應 Cloud Run 注入。內建 SPA fallback（`try_files $uri $uri/ /index.html`）解 React Router / Vue Router deep-link 404、gzip 開、nginx-conf 寫進 image。Auto-gen path only — user 帶自己 Dockerfile 走 R44h（不動）。+7 zero-dep 測試（multi-stage、SPA fallback、sh -c $PORT expansion、regression guard 防 `node dist/index.js`、PM 處理、nginx config、EXPOSE 8080） |
